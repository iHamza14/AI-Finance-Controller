## Running AI-Finance with uv

This project uses `uv` for dependency and environment management.

### Initial setup

From the `AI-Finance` directory:

```bash
cd ~/AI-Finance
```

Install and synchronize all dependencies:

```bash
uv sync
```

This creates and manages the project's virtual environment automatically at:

```text
AI-Finance/.venv
```
